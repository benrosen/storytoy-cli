/**
 * @todo {MUST} document
 * @todo {MUST} import IConfig from storytoy
 * @todo {MUST} explicitly implement IConfig
 */
export type Config = {
  author?: string;
  choices: string;
  description?: string;
  title?: string;
};

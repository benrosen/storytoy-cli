/**
 * @todo document
 * @todo import IConfig from storytoy
 * @todo explicitly implement IConfig
 */
export type Config = {
  author?: string;
  choices: string;
  description?: string;
  title?: string;
};

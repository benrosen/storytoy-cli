/**
 * Create a new choice.
 *
 * @param {string} directory The path for the new choice.
 */
export const createChoice = (directory: string) => {
  // is directory a directory?
  // is id unique in the given directory?
  // copy and populate template choice directory into target directory
  console.log(`create choice in ${directory}`);
};
